import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#16c530",
      light: "skyblue",
    },
    secondary: {
      main: "#15c530",
    },
    otherColor: {
      main: "#999",
    },
  },
});
